[project]
name = "pyeek-core"
version = "0.1.0"
description = "Pyeek のオラクル層: CPython の内部表現を JSON 化する純粋関数群のテスト環境"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.uv]
package = false
